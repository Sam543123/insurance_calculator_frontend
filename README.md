# Insurance calculator frontend

Insurance calculator frontend provides user interface for entering data for calculating insurance sum, insurance premium, reserve, tariffs table and displaying results and input errors. The frontend was written in javascript using React web framework.

## Run locally in Docker
Run docker/docker_run.cmd on windows or docker/docker_run.sh on unix. The calculator frontend will be available on http://localhost:3000/.